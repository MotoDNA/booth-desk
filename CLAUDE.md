# BOOTH DESK — 전시 부스용 방문객 접수 웹앱

## 이 프로젝트가 무엇인가

전시회 부스에서 방문객의 명함·방문증을 촬영하면 AI가 정보를 읽어 자동 입력하고,
상담 내용까지 현장에서 기록해 구글 시트로 실시간 공유하는 웹앱.

**판매용 범용 제품**이다. 특정 회사 전용이 아니며, 고객사가 설정 화면에서
자기 회사 품목에 맞게 버튼을 직접 만들어 쓰는 것이 핵심 가치다.

---

## 절대 지켜야 할 규칙

1. **단일 HTML 파일 구조를 깨지 마라.**
   `index.html` 하나에 HTML·CSS·JavaScript가 전부 들어 있다.
   빌드 도구 없음, 번들러 없음, npm 패키지 없음.
   파일을 여러 개로 쪼개자고 제안하지 마라. 고객에게 파일 하나만 주는 것이 제품의 장점이다.

2. **외부 라이브러리는 JSZip 하나뿐이다.**
   CDN으로 불러온다. React, Vue, jQuery 등 추가 금지.

3. **저장소 이름을 바꾸지 마라.**
   - IndexedDB: `boothdesk`
   - localStorage 설정 키: `bd-cfg`
   - 미러 키: `boothdesk-mirror`

   같은 GitHub Pages 도메인에 액티바코리아 전용 버전(`booth-scan` 저장소,
   IndexedDB `kprint2026`, 키 `kp-cfg`)이 따로 배포되어 있다.
   두 앱은 같은 origin이라 저장소 이름이 겹치면 **실제 전시 현장의 데이터가 덮어써진다.**
   이름 변경은 치명적이다.

4. **액티바코리아 관련 내용을 다시 넣지 마라.**
   품목명(습식PU, 지레자, MUSE, PATRIX, VIVI, FIGARO 등), 브랜드, 전시회명이
   코드에 하드코딩되면 안 된다. 전부 설정으로 빼놓은 상태다.

5. **작업 전 백업.** 수정 전 `index.html`을 복사해 둘 것.

---

## 파일 구조

```
booth-desk/
├── index.html      ← 앱 전체 (약 4,000줄)
├── CLAUDE.md       ← 이 파일
└── gs/
    └── code.gs     ← 구글 Apps Script (구글 시트 연동, 선택)
```

---

## 코드 구조 (index.html 안에서의 위치)

| 구역 | 내용 |
|---|---|
| `<style>` | CSS 변수 기반 테마. `--line`, `--sub`, `--bg` 등 |
| `<body>` | 헤더 / 접수 탭 / 명단 탭 / 설정 탭 |
| `<script>` 상단 | i18n 번역 테이블 (`ko`, `en`, `zh`) |
| `DEFAULT_PROFILE` | 항목 설정의 기본값 |
| `TEMPLATES` | 업종 템플릿 8종 |
| `P()` | 현재 프로필 반환. 모든 항목 관련 코드가 이걸 거친다 |
| `ITEMS` / `BOOKS` / `SPEC_BOOK` | `Object.defineProperty` 게터. 프로필에서 값을 읽어온다 |
| `applyLang()` | 언어 전환. 끝에서 `applyProfile()`을 호출한다 |
| `applyProfile()` | 프로필 값을 화면 글자에 반영 |
| `openDB()` ~ `dbAll()` | IndexedDB 처리 |
| `readBadge()` | Claude API 호출 (명함 판독) |
| `syncNow()` | 구글 시트 전송 및 재시도 |
| `pfInit()` 이하 | 설정 화면 동작 |

---

## 핵심 개념: 프로필(Profile)

회사마다 달라지는 것은 **오직 프로필 객체 하나**다. 코드는 건드리지 않는다.

```js
{
  brand:'예우시스템', brand2:'', sub:'부스 접수대', event:'',
  g1:{on:true, label:'관심 품목', opts:['곤도라','쇼케이스', ...]},
  g2:{on:true, label:'요청 사항', opts:['견적 요청','매장 실측 요청', ...]},
  spec:{on:true, trigger:'매장 실측 요청', label:'매장 위치 · 평수', ph:'예: 파주시 / 45평'},
  grades:[{k:'A',label:'A 즉시견적'}, {k:'B',...}, {k:'C',...}],
  locked:false
}
```

- `g1`, `g2` = 현장에서 누르는 버튼 두 묶음. 개수 제한 없음
- `spec` = `trigger`에 적힌 버튼을 누르면 나타나는 조건부 입력칸.
  트리거는 `g1`, `g2` 어느 쪽에 있어도 된다
- `grades`의 `k`는 반드시 `A`/`B`/`C`로 유지 (기존 데이터 호환)
- 프로필은 base64 코드로 내보내고 다른 기기에 붙여넣어 복제할 수 있다 (`pfEncode`/`pfDecode`)

**새 업종 템플릿을 추가할 때**는 `TEMPLATES` 객체에 항목을 하나 더 넣으면 된다.
`spec.trigger`는 반드시 `g1.opts` 또는 `g2.opts` 안에 실제로 있는 문자열이어야 한다.
없으면 저장 시 검증에서 막힌다.

---

## 데이터 흐름

```
촬영 → 이미지 축소 → Claude API(Vision) 판독 → 필드 자동 입력
     → 버튼 선택·등급·메모 입력 → IndexedDB 저장
     → 구글 Apps Script로 POST → 구글 시트에 행 추가
     (전송 실패 시 대기열에 남았다가 연결 복구되면 자동 재전송)
```

- 사진은 기기 안에만 저장된다. 외부로 나가는 것은 판독용 이미지와 텍스트뿐
- 개발자 서버·중앙 DB 없음
- Claude API 키는 사용자가 설정 화면에 직접 입력하고 localStorage에 저장된다
  (배포 파일에 키를 절대 넣지 마라)

---

## CSV 열 이름 (변경 시 주의)

```
번호, 접수시각, 담당자, 회사명, 성함, 직함, 전화번호, 이메일, 회사주소,
등급, 관심항목, 요청사항, 추가입력, 메모, 사진파일명
```

`관심항목`과 `요청사항`은 여러 값을 ` / `로 이어 붙인 한 칸이다.
버튼이 몇 개로 늘어나도 열 구조가 깨지지 않게 하려는 의도적 설계다.
**열마다 품목을 하나씩 배정하는 구조로 되돌리지 마라.**

가져오기(`bulkImport`)는 구버전 열 이름(`관심품목`, `샘플북요청`, `속지품번`)도
함께 받아들인다.

---

## 배포 방법

GitHub Pages 정적 호스팅.

1. `index.html`을 저장소에 올린다
2. Settings → Pages 활성화
3. 접속: `https://<계정>.github.io/booth-desk/`

**캐시 주의:** 수정 후에도 옛 화면이 보이면 주소 뒤에 `?v=2` 처럼 붙여서 연다.
홈 화면에 추가해 둔 아이콘은 업데이트 후 삭제하고 다시 추가해야 한다.

---

## 테스트 방법

브라우저에서 직접 열어 확인하는 것이 가장 빠르다. 확인할 것:

1. 설정 탭 → 업종 템플릿 8종을 차례로 불러오고 저장 → 버튼이 제대로 바뀌는가
2. 조건부 입력칸이 트리거 버튼 클릭 시 나타나는가
3. 설정 코드 복사 → 붙여넣기 적용 → 원래대로 돌아오는가
4. 접수 → 저장 → 명단 탭에 표시되는가
5. CSV 내보내기 → 엑셀에서 한글이 깨지지 않는가 (UTF-8 BOM)
6. 비행기 모드에서 저장 → 연결 복구 시 자동 전송되는가

---

## 하지 말아야 할 제안

- 파일 분리, 빌드 도구 도입, 프레임워크 도입
- 회원가입·로그인 기능 추가 (제품 성격상 불필요)
- 중앙 서버 도입 (개인정보 보관 책임이 생긴다)
- 저장소 이름 변경
- 액티바 전용 버전(`booth-scan`)과의 코드 통합

---

## 앞으로 할 일

- [ ] 구글 Apps Script(`code.gs`)의 열 이름을 범용 이름으로 맞추기
- [ ] 업종 템플릿 추가 (물류·설비, 식품, 의료기기 등)
- [ ] 무료 체험 30건 제한 기능
- [ ] 전시 종료 후 명단 요약 화면

---

## Ver 3.0 (B2B) 작업 중

판매용 Ver 2.0(`index.html`)은 위 규칙 그대로 유지하고,
B2B 확장은 `booth-desk-b2b.html`에서 따로 진행합니다.

**Ver 3.0 작업을 이어받는다면 `RESUME.md`를 먼저 읽으세요.**
